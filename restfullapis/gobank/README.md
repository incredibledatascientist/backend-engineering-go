1. I have api.go and main.go file with same main package

- If we make build then run it works because go compiles all the files at once.
- But when we run main.go it only runs current file, then to run we have to user [go run .] | [go run *.go]
