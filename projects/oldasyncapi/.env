// .env → Stores environment-specific values and secrets (DB passwords, API keys, ports)
//  that change per deployment and should not be committed.

export DB_USER=postgres
export DB_PASSWORD=infierms 
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=asyncapi
